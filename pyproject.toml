[project]
name = "LightningBO"
version = "0.1.0"
description = "Bayesian optimization core library"
requires-python = ">=3.12"

license = "Apache-2.0"
dependencies = [
	"jax",
	"jaxlib",
	"optax",
	"numpy",
	"absl-py",
	"pytest",
	"matplotlib"
]
